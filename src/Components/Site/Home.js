import React from 'react';
import { Card } from '@material-ui/core';

const Home = () => {
    return(
        <Card>
            <h1>Greetings from the Home page</h1>
        </Card>
    )
};

export default Home